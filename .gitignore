node_modules
dist
dist-ssr
*.local
.DS_Store
*.tsbuildinfo

# iOS は macOS 必須。ここでは CI(macOS ランナー)で都度生成するため ios/ 全体を除外。
# Mac でローカル開発しコミットしたくなったらこの行を外す。
ios/

# Capacitor Android（生成物のみ除外。ソースは管理）
android/.gradle
android/app/build
android/build
android/local.properties
android/.idea
*.log
.vite

# Claude Code のマシンローカル設定（個人ごとの許可リスト等）
.claude/settings.local.json
